import React, { ReactElement } from 'react'

interface Props {
    
}
const Footer = ({}: Props): ReactElement => {
    return (
        <div>
            Footer
        </div>
    )
}

export default Footer;